services:
  - type: web
    name: nova
    runtime: static
    buildCommand: npm install
    staticPublishPath: ./
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    headers:
      - path: /*
        name: Cache-Control
        value: no-cache
